# Core
ollama>=0.3.0
streamlit>=1.35.0
pandas>=2.0.0
numpy>=1.24.0                 # directly used by the hallucination guardrail
plotly>=5.20.0                # dashboard charts (donut / bar), dark-theme styled

# ML / NLP
torch>=2.0.0
transformers>=4.40.0
sentencepiece>=0.2.0          # tokenizer backend for MuRIL / IndicNER
protobuf>=4.0.0

# Hinglish transliteration (Roman -> Devanagari)
indic-transliteration>=2.3.0

# REST API layer (api.py) — runtime, not just dev, because the Docker image
# and deployment docs run `uvicorn api:app`.
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Optional: grounding / NLI hallucination checks (POST /check_grounded and
# `evaluate.py --output-mode` grounding metrics). The pipeline degrades
# gracefully to "grounded/unavailable" when this is absent, so it is not
# required for the core guardrails. Uncomment to enable the grounding feature:
# sentence-transformers>=2.7.0
