# Testing
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27          # required by FastAPI TestClient

# FastAPI (needed for the API layer and its tests)
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Code quality
black>=24.0
ruff>=0.4
