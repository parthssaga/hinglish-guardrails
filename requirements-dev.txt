# Testing
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27          # required by FastAPI TestClient
# NOTE: fastapi + uvicorn are runtime deps (see requirements.txt); the
# TestClient just needs httpx on top of them.

# Code quality
black>=24.0
ruff>=0.4
